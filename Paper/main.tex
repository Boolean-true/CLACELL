\documentclass[11pt]{article}

\input{preamble} % If needed, you can load additional packages or define your own macros in the preamble.tex file

\title{CLACELL: Robust cell type classifier for immune cells}
\author{Julian Kraus}

\affil{
%[Master's / Bachelor's] thesis in [your study degree]\\ % For theses
%Direct supervisor: [Dr. Anne Hartebrodt]\\ % For theses, delete if directly supervised by David
%Supervising professor: Prof. Dr. David B. Blumenthal\\ % For theses
Project report\\ % For project reports in the module Project Biomedical Network Science
%Student paper\\ % For student papers in the module Network Medicine
Module title: Project Biomedical Network Science\\% For project reports and student papers
Semester: SS2026\\% For project reports and student papers
Matriculation number: 23766907\\
Biomedical Network Science Lab, Department Artificial Intelligence in Biomedical Engineering, Friedrich-Alexander-Universität Erlangen-Nürnberg}

\date{Submission date: [date]}


\begin{document}


\maketitle

\begin{abstract}
Write your abstract here. An abstract is a brief one-paragraph summary that motivates your work, and provides a high-level description of why it is relevant. Excellent guidelines can be found here: \url{https://www.nature.com/documents/nature-summary-paragraph.pdf}. Your abstract should fit on the first page of this document.
\end{abstract}

\clearpage

\tableofcontents

\clearpage

\section{Introduction}\label{sec:introduction}

%\subsection{Background}\label{sec:introduction:background}

TODO: Describe the role of scRNA-seq in immunology and the challenge of cell type annotation.

%\subsection{State of the Art \& Limitations}\label{sec:introduction:soa}

TODO: Briefly discuss marker-based approaches like scumi (tedious, manual) and existing automated tools like CellTypist. Maybe mention the instability of standard Deep Learning approaches for this task.

There exist many approaches to solve the cell type annotation problem, including marker-based annotation, traditional machine learning, and deep learning. %TODO: Etwas zu marker based schreiben
%However, these approaches have limitations in terms of accuracy, robustness, and interpretability. Marker-based annotation is often tedious and requires expert knowledge, while traditional machine learning methods may not capture complex relationships in the data. Deep learning approaches can be unstable and require large amounts of labeled data for training.
While deep learning approaches might seem to be the best option for this task, \textcite{boiarsky_deep_2023} show that deep learning approaches are not necessarily superior to traditional machine learning methods for cell type annotation. In fact, they show that simple machine learning methods can compete with deep learning methods.
%TODO: noch auf Robustness, Interpretierbarkeit und evtl Datenlimitierung eingehen

%\subsection{My Contributions}\label{sec:introduction:contributions}

TODO:
- Python re-implementation of the scumi R package
- Comprehensive benchmark of traditional ML, Denoising Autoencoders, and Custom Ensemble methods (with Feature Subsampling)
- Publication of a ready-to-use Python package (with current version of python and scikit learn)

This paper presents a re-implementation of the scumi R package in Python, which allows for marker-based annotation of immune cells natively in Python. Additionally, we provide a comprehensive benchmark of traditional machine learning methods, denoising autoencoders, and custom ensemble methods with feature subsampling. Our work aims to improve the accuracy, robustness, and interpretability of cell type annotation methods. Finally, we release a ready-to-use Python package that implements our methods and can be easily integrated into existing workflows. %TODO: Nochmal drüberlesen und anpassen; Evtl nicht die Reimplementierung hier erwähnen? Ist ja kein Kernfokus der Arbeit


% TODO: Adjust the Introduction section according to this section from the template:
%In the introduction, you provide the motivation of your work, pose your research question, and describe the (medical and technical) background as well as the related work (if these parts are rather extensive they can also be put in dedicated ``Background'' and/or ``Related work'' sections). Make sure that the contribution of \emph{your} work becomes clear in the introduction: What does it add to the existing body of literature/knowledge? Why is it new and/or interesting? Conclude the introduction by delineating the structure of your thesis, i.\,e., by introducing the structure of the remaining sections. The introduction should be internally unstructured (no subsections inside the introduction). 


\section{Results}\label{sec:results}

\subsection{Structure of the Results section}\label{sec:results:overview}

%The first subsection of the \nameref{sec:results} is often used to provide a high-level overview of the entire work. This is typically supported by a visual overview in \Cref{fig:overview-placeholder}. The overview should enable readers to understand the material presented in the subsequent subsections of the \nameref{sec:results} section. The methodological details are then explained in a dedicated \nameref{sec:methods} section.

%The remainder of the \nameref{sec:results} is typically structured into topical subsections. As a rule of thumb, each subsection should present one key finding, supported by one data figure. Of course, you can divert from this rule of thumb if it does not fit for your work.

%TODO: visual overview of teh results like mentioned above

We divided the results section into three main parts: first, we compare different machine learning algorithms and present their results. Second, we evaluate Ensemble Learning models and Autoencoders. Finally we compare the best model from the first two parts with the state of the art models CellTypist, scGPT and SinleR. %TODO: Passt das?; Brauche ich diese Subsection?; Besser formulieren

\subsection{Comparison of Machine Learning Algorithms}\label{sec:results:ml}

%TODO: Technical Specs and Random Dropout Score (everything from the table)

%As a comparison of the robustness of the models we included a diagram that shows how much the Accuracy score declines when more and more important features are dropped from the input data. 
As a comparison of the robustness of the models we plotted the baseline Accuracy score and the score for the feature importance dropout in a line plot. This shows how much the Accuracy score declines with an increasing number of missing important features. %TODO: nennt man das einen line plot?; Besser formulieren

The results of the comparison of this comparison are shown in \Cref{fig:id-ml-robustness-comparison} for the in-distribution test set and in \Cref{fig:ood-ml-robustness-comparison} for the out-of-distribution test set. The results show that the linear models Logistic Regression and LinearSVC perform better than the tree-based models Random Forest, Extra Trees and LightGBM. When the tree-based models are better than the linear models, they are only slightly better, however the other way around the linear models are significantly better than the tree-based models. LinearSVC is generally the best performing model, while LightGBM performs worst of all models.
%TODO: Besser formulieren; Evtl. noch mehr zu den Ergebnissen schreiben

\begin{figure}[htb]
    \centering
    \includegraphics[width=\linewidth]{figures/in\_distribution\_ml\_robustness\_comparison.png}
    \caption{In Distribution Robustness Comparison of Machine Learning Models}
    \label{fig:id-ml-robustness-comparison}
\end{figure}

\begin{figure}[htb]
    \centering
    \includegraphics[width=\linewidth]{figures/out\_of\_distribution\_ml\_robustness\_comparison.png}
    \caption{Out-of-Distribution Robustness Comparison of Machine Learning Models}
    \label{fig:ood-ml-robustness-comparison}
\end{figure}

%TODO: Grafiken evtl kleiner machen

%TODO: Evtl nur out of distribution Grafik im Text anzeigen und in distribution Grafik in den Anhang packen

%TODO: Schlussfolgerung, welches Modell am Besten abschneidet und weiter getestet wird

%TODO: Subsection drüberlesen


\subsection{Comparison of Ensemble and Denoising Approaches}\label{sec:results:ensemble-ae}

TODO

\subsection{Comparison with Reference Models}\label{sec:results:reference-models}

TODO


\section{Discussion}\label{sec:discussion}

%The discussion typically starts with a summary of your work's most important results. Then, you discuss limitations of your work and potential follow-up. Finally, you end on a positive note and tell the reader at a very high abstraction level how your work advanced the field: What did we learn through your work? What do we know now that was not known before? Which type of analyses can now be run now that were not possible before? 

%Typically, discussions are internally unstructured. But you may deviate from this rule of thumb, e.\,g., by including separate subsections on limitations and outlook.

TODO

\section{Methods}\label{sec:methods}

%Good Methods sections are designed in such a way that the subsection headings allow the reader to quickly navigate it without reading the entire section from top to bottom. You may think of it as a dictionary where readers look up details of the work that are of particular interest to them. Each individual subsection should be detailed enough to allow replication of your work, so be precise in your descriptions. Overall, the subsections of your Methods section should cover all methodological details that are relevant for your work: details on your ML model or algorithm, descriptions of datasets and data preprocessing, details on evaluation protocols and metrics, description of competitor approaches, etc.

\subsection{Data Preprocessing}\label{sec:methods:data-preprocessing}

%TODO: Maybe describe the datasets (Number of Samples, Number of Features)

The preprocessing described in this section was applied to both the in-distribution and out-of-distribution datasets. The preprocessing pipeline was designed to ensure that the datasets are comparable and that the models can be trained and evaluated effectively.

Before we started with the preprocessing, we made sure to use the raw counts of the datasets. As we later normalize the data, already normalized data would have been a problem. We also filtered the in distribution dataset for blood data, as it contained more data. The out-of-distribution dataset already only contains blood data.

First we filtered mitochondrial, ribosomal and hemoglobin genes from the datasets, as they are not informative for cell type classification. %TODO: Stimmt das, Quelle; qc metrics erwähnen?
After that we removed doublets as they can introduce noise and bias into the data. %TODO: Stimmt das, Quelle?

Then we nomralized the data to a target sum of $1e4$ and log transformed it. This is a common preprocessing step in single-cell RNA sequencing data analysis, as it helps to stabilize the variance and make the data more normally distributed. %TODO: Stimmt letzter Satz + Quelle

Finally we filtered the datasets for highly variable genes, as they are more informative for cell type classification. 
In order to determine the number of highly variable genes to use, we created an elbow plot that shows the variance of the genes in the dataset. The elbow plot is shown in \Cref{fig:elbow-plot-gene-count}. It shows that until around 10000 genes the biological variance is still grater than 0. Additionally we trained Random Forest models to determine which threshold to use. Despite the low variance at 10000 genes, the Random Forest models performed best with this threshold. Therefore we decided to use the top 10,000 highly variable genes for our analysis. %TODO: Besser beschreiben?

\begin{figure}[htb]
    \centering
    \includegraphics[width=\linewidth]{figures/gene\_count\_elbow\_plot.png}
    \caption{Elbow-Plot for Comparison of Gene Count}
    \label{fig:elbow-plot-gene-count}
\end{figure}

%TODO: Ganze Subsection nochmal drüberlesen und überarbeiten, evtl. noch mehr Details zu den einzelnen Schritten

\subsection{Reference Models}\label{sec:methods:reference-models}

We compare our results to two state of the art models, CellTypist and scGPT. Both use different approaches so the comparison is meaningful. %TODO: Besser beschreiben; ist CellTypit State of the Art?

CellTypist is based on a logistic regression and offers stochastic gradient descent. It was originally trained on the whole Cross-tissue Immune Cell Atlas dataset with more than just blood cells. We retrained it on the filtered training dataset to make it comparable to our models. For the training we build a wrapper around the CellTypist model so that it uses the Scikit-learn API and can then be tuned the same way as our models. We used the \texttt{RandomizedSearchCV} from Scikit-learn with 30 Iterations and a Cross Validation of 5 to optimize the hyperparameters. %TODO: Mehr schreiben?; Quellen

%scGPT is a transformer-based model for single-cell RNA sequencing data analysis. %ODER
scGPT uses a Deep Learning approach and was trained on a large dataset of single-cell RNA sequencing data. 
%It is designed to learn a representation of the data that captures the underlying biological processes and can be used for various downstream tasks, including cell type annotation. %TODO: So eine Beschreibung geben? Dan mit Quelle
We fine tuned the pre-trained scGPT model for our comparison, as it was not feasible to retrain it on our dataset due to its size and complexity. %TODO: Passt das nach dem Komma?
We used the model that was pre-trained on blood data as we only have blood data in our datasets. %TODO: Besser in den Absatz einbauen
To finetune scGPT we adjusted the finetuning script provided by the authors on github. Again we build a wrapper to use the Scikit-learn API for our robustness tests. %TODO: Quelle; hier oder unter kein hyperparametertuning?
We didn't perform any hyperparameter tuning for the fine tuning, as it was not feasible to do so due to the size and complexity of the model. %TODO: Besser in den Absatz einbauen; besser formulieren; Dopplug zu oben
As scGPT did not perform well at the beginning of our experiments, we made some adjustments to the finetuning script to improve its performance. A major issue was that classes with few samples were not learned well. On top of that a ranom train valid split was made on the train data, so there was a chance that some classes are not represented in the train split. To counter that, we added a stratified cross validation to the split. This also ensured that the proportion of classes in the train and validation split is the same as in the whole dataset. 
The stratified cross validation improved the performance of scGPT, but it still did not perform well on classes with few samples. Therefore we additionaly added sampling to the training data to ensure that each class is sufficiently represented in the training data. This was done by upsampling classes with less than 1000 samples. %TODO: Mehr dazu erklären? Vor allem zum letzten Satz?
These changes lead to a significant improvement in the performance of scGPT. %TODO: Irgendwie noch schreiben, dass ich jetzt scGPT mit diesen Changes verwendet habe
%TODO: Waren das schon alle Anpassungen?
%TODO: Überarbeiten!

\subsection{Marker-Based Annotation}\label{sec:methods:marker-based-annotation}

%TODO: Describe scumi and the re-implementation in Python. Describe how it is used to generate the ground truth labels for training and testing. Maybe mention here the runtime and RAM consumption of the reimplementation (alternatively in the Results section).

For the annotation of the in- and out-of-distribution dataset, we used the marker based annotation startegy scumi. %TODO: scumi irgendwie hervorheben mit textit oder so?; Zitat auf Scumi; Evtl. noch beschreiben wie scumi funktioniert
As it was only available as a R package, we re-implemented it in Python. For the labeling of the datasets we used the marker genes provided by scumi.
%TODO: Überarbeiten, klingt komisch

%TODO: Absatz mit Beschreibung zur Reimplementierung

%TODO: Evtl Absatz mit runtime und RAM consumption der Reimplementation (alternativ in Results section)

\subsection{Machine Learning Algorithms}\label{sec:methods:machine-learning}

To classify the immune cells based on their gene expression profiles, we evaluated several machine learning models, categorized into linear and tree-based algorithms. For the linear approaches, we implemented Logistic Regression and a Linear Support Vector Classifier (LinearSVC). For tree-based models, we used Random Forest, Extra Trees and LightGBM.

The hyperparameter search spaces for each model are detailed in \Cref{tab:ml_hyperparameters}. To ensure compatibility with different loss functions and improve convergence, we used the saga solver with a maximum of 1000 iterations for Logistic Regression for all iterations. LightGBM's objective was fixed to multiclass for our evaluation, as it is a multiclass classification problem. Furthermore, the number of parallel jobs was set to 1 as the hyperparameter tuning algorithm already parallelizes the training of the models.

Hyperparameter tuning was performed using Bayesian Optimization via \texttt{BayesSearchCV} from the scikit-optimize library \cite{scikit-optimize_contributors_skoptbayessearchcv_2020} with 30 iterations and a 5-fold cross-validation. Compared to traditional grid or random search strategies, Bayesian optimization offers enhanced efficiency by using past evaluation results to find better parameter choices, thereby finding optimal configurations faster. \cite{NIPS2012_05311655}

To optimize computational resources, a custom early-stopping mechanism was integrated into the optimization. This mechanism terminates the search if the model's performance failed to improve by more than 0.2 \% over 5 consecutive iterations. To ensure sufficient initial exploration of the hyperparameter space, this stopper was restricted from triggering until the 15th iteration. Consequently, if no significant improvement was observed between iterations 10 and 15, the search was terminated immediately at the 15th iteration, preventing unnecessary computational overhead. %This approach substantially reduced computational overhead without compromising the quality of the final hyperparameter configurations. %TODO: Diesen Satz noch schreiben?


\subsection{Ensemble Learning Strategies}\label{sec:methods:ensemble-learning}

%TODO: Standard Voting and Stacking Classifiers as well as my custom Feature-Subsampling Voting Ensemble (explain how features were subsampled and why this increases robustness).

The best Machine Learning models were combined into ensemble learning strategies to improve the overall performance and robustness of the classification. We used standard voting and stacking classifiers, as well as a custom feature-subsampling voting ensemble.

For the voting classifier,the base estimators as well as the voting type (soft or hard) were optimized using a grid search. The models were tested with little hyperparameter tuning as well as with the optimal hyperparameters from the tuning of the single models. This gives a comparison of the performance of the ensemble methods with hardly tuned and optimal tuned hyperparameters. %TODO: Überarbeiten! Evtl statt dem letzten Satz eine Begründung warum ich einmal mit wenig und dann mit viel hyperparameter tuning getestet habe

%TODO: Etwas zu stacking schreiben

In the custom ensemble, we combined the same machine learning model mutliple times, each time trained on a different subset of features. The subsets of features include the full set of features and three subsets of features where the top features according to a feature importance ranking were removed. This allows the ensemble to be more robust to missing features and to better generalize to new data. The include the removal of the top 0.75 \%, 1.5 \% and 2.5 \% of the features according to the feature importance ranking.
For the linear models we used the feature importance of the random forest model, while for the tree-based models we used the feature importance of the logistic regression model. 
By using the feature importance of a different machine learning approach, we avoid the bias of the feature importance ranking towards the model that is used for the ensemble. Furthermore the learnings from two different approaches are combined to improve the overall performance. %TODO: Evtl. noch besser erklären

%TODO: Subchapter überarbeiten!

\subsection{Denoising \& Hybrid Approaches}\label{sec:methods:denoising-hybrid}

TODO: the Denoising Autoencoder + Conditional Autoencoder combined with Logistic Regression. Mention why this approach can help with robustness and why the conditional autoencoder helps even further.

\subsection{Robustness Tests}\label{sec:methods:robustness-tests}

The robustness evaluation of the models contains two parts with the same tests: first, models are evaluated on an in-distribution test set, followed by an assessment on an out-of-distribution test set. Comparing model performance on both in-distribution and out-of-distribution test sets allows us to measure their generalization capabilities and robustness against distribution shifts. %TODO?: Passt das?; Mehr zu dem ID OOD Vergleich schreiben?

The in-distribution test set is a hold-out test set from the same base dataset as the training data. To prevent data leakage, the dataset split was performed on the donors, ensuring that no donor is present in both the training and the test set. The donors for the test set were chosen so that the number of samples in the test set is between 20 \% and 35 \% of the total samples. Out of all valid splits fulfilling this criterion, we selected the one closest to the mean of 27.5 \%. Due to the highly variable number of samples per donor in the base dataset, the final split resulted in a test set size of 33.7 \% of the total samples.

While strictly necessary to avoid data leakage, this donor-stratified strategy induced a severe class imbalance, leaving Dendritic cells and Megakaryocytes heavily underrepresented in the final test set. %TODO: Anzahl Samples erwähnen? with 5 and 7 samples respectively
Given their low representation in the base dataset, a more balanced split was mathematically impossible under the donor-stratification constraint. The results of these classes will be compared to the results on the out-of-distribution test set, which contains a more representative number of samples for these cell types.
%TODO: Ist das zu viel Text?
%TODO: hier erwähnen oder woanders?

The out-of-distribution test set contains cells from an independent source. To ensure comparability and a valid evaluation, it was preprocessed in the same way as the training dataset. %TODO: Mehr zum OOD dataset schreiben? Evtl die Unterschiede zum Trainings dataset; Den zweiten Satz habe ich schon beim Preprocessing erwähnt, trotzdem hier lassen?
%The OOD test set consists of cells sourced from a completely independent dataset. To ensure comparability and a valid evaluation, the OOD data underwent the identical preprocessing pipeline as the training dataset.


As an initial baseline, the performance of the models is evaluated on the full test sets for each distribution. While overall accuracy provides a primary indication of model performance, it is insufficient given the class imbalance of the base dataset. Therefore, we additionally compute a comprehensive classification report including precision, recall, F1-score, and support for each individual class, alongside macro and weighted averages. This allows us to assess class-specific model performance.

%After assessing the baseline performance of the models, we perform a robustness test by adding noise to the input data. The noise is added to the input data by randomly selecting 10 \% of the features and replacing their values with 0. This allows us to assess how well the models can handle noisy data and how their performance degrades with missing features.

%Another robustness feature the models are tested on is the number of inconsistent predictions when predicting the same data multiple times. We perform this test by predicting the same data 5 times and counting the number of inconsistent predictions over all runs. This allows us to assess how stable the models are and how much their predictions vary with different runs. %TODO: Doppelt sich da nicht was im letzten Satz?; evtl besser begründen

%The last robustness test is the most challenging one, as it removes the most important features from the input data and computes the accuracy of the model. The most important features are determined by the marker genes that were obtained by scumi. However, since there aren't enough marker genes, the rest of the important features are determined by a differential gene expression on the training dataset. %TODO: Besser erklären
%The feature importance dropout test is performed four times with different percentages of the most important features removed: 0.1 \%, 0.5 \%, 1 \% and 2 \%. Again the values of the removed features are set to 0. This allows us to assess how well the models can handle missing important features and how their performance degrades with increasing feature dropout. It also shows how much each model relies on the most important features for its predictions.


%ODER
%TODO: Entscheiden und gegebenenfalls den Text oben überarbeiten

Following the baseline evaluation, we introduce three distinct robustness tests to challenge the models:

\begin{enumerate}
    \item \textbf{Random Dropout:} We simulate noisy data by randomly selecting 10 \% of the input features and setting their values to 0. This test quantifies how the well the model can handle noisy data and how its performance degrades when faced with missing features.
    \item \textbf{Prediction Stability:} To evaluate prediction consistency under stochastic inference, we execute predictions on the same dataset across five independent runs. The number of inconsistent predictions across these iterations is aggregated to measure the stability and variance of the models.
    \item \textbf{Feature Importance Dropout:} The final and most challenging test evaluates model resilience against the loss of highly informative biological signals. We progressively remove the most critical features by setting their values to 0 at four distinct thresholds: 0.1 \%, 0.5 \%, 1 \% and 2 \% of the total feature space. The feature importance for the Dropout was determined primarily by the marker genes identified by scumi. Since the number of these marker genes was insufficient to meet the higher percentage thresholds, the remaining top features were supplemented via differential gene expression analysis computed on the training set. This test reveals how heavily each model relies on primary marker genes and how robustly they utilize alternative features when key biological markers are missing. It also assesses how the performance of each model degrades as the proportion of removed important features increases.
\end{enumerate}

%TODO: Mehr zu den Tests schreiben? Mehr Details?
%TODO: Über die ganze subsection nochmal drüberlesen


\section{CLACELL Package}\label{sec:package}

In this section we provide an overview and a walkthrough of the CLACELL package, which implements our best model, the custom ensemble with the LinearSVC. The package is designed to be user-friendly and easily integrated into existing workflows for immune cell type classification. %TODO: Den letzten Satz schreiben oder weglassen?

The package can be installed via pip from the Python Package Index (PyPI) using the following command:
\begin{lstlisting}
pip install clacell
\end{lstlisting}
%TODO ODER: The package can be installed via pip or uv.

\subsection{Annotation}\label{sec:package:annotation}

To train the CLACELL model, a labeled dataset is required. The package provides the class \texttt{MarkerAnnotator} for this purpose. It is a wrapper around the re-implementation of the scumi R package we presented in \Cref{sec:methods:marker-based-annotation}, which allows users to annotate their datasets with a given marker gene dictionary. To annotate a dataset, the user calls the method \texttt{annotate} with the dataset as an input. It returns the annotated dataset with the cell type labels in the field \texttt{adata.obs['scumi-annotation']}. The annotated dataset can then be used to train the CLACELL model. The annotation process is illustrated in \Cref{lst:clacell-annotation}.

\begin{lstlisting}[language=Python, caption=Annotation workflow with the CLACELL package, label={lst:clacell-annotation}]
from clacell import MarkerAnnotator

annotator = MarkerAnnotator(marker_dict=marker_dict)
adata = annotator.annotate(adata)
# Now adata.obs['scumi-annotation'] contains the annotations
\end{lstlisting}

%TODO: Subsection überarbeiten; Sollte ich wirklich solche Codeblöcke einfügen?


\subsection{Preprocessing}\label{sec:package:preprocessing}

Before training the CLACELL model, the input dataset needs to be preprocessed. Our package provides a sample preprocessing pipeline introduced in \Cref{sec:methods:data-preprocessing}. 
%The preprocessing steps include filtering of mitochondrial, ribosomal and hemoglobin genes, removal of doublets, normalization to a target sum of $1e4$, log transformation, and filtering for highly variable genes. %TODO: Nochmal erwähnen?
However, this preprocessing pipeline is not mandatory, and users can choose to implement their own preprocessing steps if desired. It should include a normalization, log transformation and filtering for highly variable genes, as these steps are crucial for the performance of the CLACELL model.

After preprocessing, the dataset should be split into a training and a test set in order to train and evaluate the model. We recommend a split among the batch or donor level to avoid data leakage. The test set size should be between 20 \% and 35 \% of the total samples.

As the CLACELL model requires the training and test sets to be in a specific format, both sets should be converted into a dataframe and the cell type labels should be stored in a separate series.

The Listing \ref{lst:clacell-preprocessing} shows an example of how to preprocess a dataset and split it into training and test sets using the CLACELL package.

\begin{lstlisting}[language=Python, caption=Preprocessing workflow with the CLACELL package, label={lst:clacell-preprocessing}]
from clacell import preprocess_data

adata_preprocessed = preprocess_data(adata)

# Split the preprocessed data into training and test sets
donor_train = ['637C', 'A35', 'A36', 'D503']
donor_test = ['621B', 'D496']
adata_train = adata[
    adata.obs["Donor"].isin(donor_train)
].copy()
adata_test = adata[
    adata.obs["Donor"].isin(donor_test)
].copy()

# Prepare Data for training
X_train = adata_train.to_df()
y_train = adata_train.obs['scumi-annotation']

X_test = adata_test.to_df()
y_test = adata_test.obs['scumi-annotation']
\end{lstlisting}

%TODO: Subsection überarbeiten; Sollte ich wirklich solche Codeblöcke einfügen?


\subsection{Classification}\label{sec:package:classification}

The Classifier \texttt{CellClassifier} from the CLACELL package implements the custom ensemble with the LinearSVC. It provides four methods:

\begin{itemize}
    \item \texttt{def grid\_search(X\_train, y\_train, X\_test, y\_test, labels, n\_jobs)}: This method makes a random search with 50 iterations to find the optimal hyperparameters for the LinearSVC. Then it trains the custom ensemble on the training data and evaluates it on the test data. Afterwards, it calls the train method with the optimal hyperparameters and the combined training and test data. It takes as input the features \texttt{X} and corresponding labels \texttt{y} for the training and test sets, the field where the labels are stored and the number of jobs for parallel processing. %TODO: Anpassen, wenn ich die Funktion anpasse!
    \item \texttt{train(X\_train, y\_train, **hyperparameters)}: This method trains the custom ensemble with the given hyperparameters on the given data. It takes as input the features \texttt{X} and corresponding labels \texttt{y} for the training set, as well as the hyperparameters for the LinearSVC.
    \item \texttt{evaluate(X\_test, y\_test, labels, X\_ood, y\_ood, feature\_importances, log\_to\_console, log\_to\_file)}: This method evaluates the model's performance on a in distribution and out of distribution test set. It is a wrapper around the tests presented in \Cref{sec:methods:robustness-tests}. It takes as input the features \texttt{X} and the corresponding true labels \texttt{y} for the in and out of distribution test sets, the Feature Importance and two flags if the results should be logged to the console and/or file. It can only be called if the model has been trained. It returns a multi index dataframe with the robustness report.
    \item \texttt{predict(X)}: This method predicts the cell types for the given input features \texttt{X}. It can only be called if the model has been trained. It returns the predicted labels.
\end{itemize}
%TODO: Überarbeiten!; Wirklich alle Parameter so sehr beschreiben?

The Listing \ref{lst:clacell-classification} shows an example of how to train and evaluate the CLACELL model using the CLACELL package.

\begin{lstlisting}[language=Python, caption=Classification workflow with the CLACELL package, label={lst:clacell-classification}]
from clacell import CellClassifier

classifier = CellClassifier()

# 1. GridSearch
print("\n1. grid_search")
classifier.grid_search(X_train, y_train, X_test, y_test, 
     "scumi-annotation", n_jobs=3)

# 2. Train
print("\n2. train")
classifier.train(X_train, y_train, C=0.001)

# 3. Evaluate
print("\n3. evaluate")
# X_ood, y_ood and feature_importance are assumed to be 
# defined previously
classifier.evaluate(X_test, y_test, "scumi-annotation", 
     X_ood, y_ood, feature_importance, log_to_console=True,
     log_to_file=True)

# 4. Predict
print("\n4. predict")
predictions = classifier.predict(X_test)
print(f"Macro F1: {f1_score(y_test, predictions, average="macro")}")
\end{lstlisting}

%TODO: Subsection überarbeiten; Sollte ich wirklich solche Codeblöcke einfügen?


\section{Code availability}\label{sec:code}

If you wrote any source code as part of your work, provide a link to a well-documented GitHub repository here. Moreover, release a stable version of your source code and provide its DOI here. You can use Zenodo for this, see \url{https://help.zenodo.org/docs/github/}. Write ``Not applicable'' if and only if you did not write any source code as part of your work.


\section{Data availability}\label{sec:data}

The Dataset used for training and in distribution testing is the Cross-tissue Immune Cell Atlas that is provided by the Teichmann Group at the Wellcome Sanger Institute hosted by Genome Research Limited (licensed under CC BY-NC-ND 4.0). It is publicly accessible at \url{https://www.tissueimmunecellatlas.org/}. %TODO: Noch genauer beschreiben welchen Datensatz ich verwendet habe?
\cite{dominguez_conde_cross-tissue_2022} %TODO: Oder Zitat in den Text verschieben nach den Namen?

For the out-of-distribution testing, we used the dataset ``A single cell immune cell atlas of human hematopoietic system'' (Project Label: ``HematopoieticImmuneCellAtlas'') that is provided by the Human Cell Atlas Data Portal (licensed under CC-BY 4.0). It can be accessed at \url{https://explore.data.humancellatlas.org/projects/cc95ff89-2e68-4a08-a234-480eca21ce79}.
\cite{Regev2025Hematopoietic} %TODO: Oder Zitat in den Text verschieben nach den Namen?

%Describe from where you obtained the public data underlying your work. Your descriptions should be detailed enough to allow others to access the exact same data and to reproduce your results, using the code you linked in \Cref{sec:code}. If you generated data yourself (e.g., through simulation), either deposit the data in a public repository such as Zenodo and provide the DOI here or explain how others can rerun the simulation to generate the data themselves. If you used private or sensitive data that is not public and cannot be made publicly available, explain why this is the case. Write ``Not applicable'' if and only if you did not use any data for your work.

\printbibliography

\clearpage

\appendix
\renewcommand{\thefigure}{S\arabic{figure}}
\renewcommand{\thetable}{S\arabic{table}}
\setcounter{figure}{0}
\setcounter{table}{0}
\crefalias{section}{appendix}
\crefname{appendix}{appendix}{appendices}
\Crefname{appendix}{Appendix}{Appendices}

\section{Supplementary figures}\label{appendix:figures}

Supplementary figures that provide additional results but do not fit into the narrative of the main text can be put here. All supplementary figures should be referenced from the main text and should be listed here in the order of these references. Delete this section if not needed.

\begin{figure}[htb]
    \centering
    \includegraphics[width=0.5\linewidth]{example-image-b}
    \caption{Caption of \Cref{fig:supp-fig}.}
    \label{fig:supp-fig}
\end{figure}

\section{Supplementary tables}\label{appendic:tables}

\begin{table}[htbp]
\centering
\caption{Hyperparameter search spaces and fixed configurations for the evaluated machine learning models.}\label{tab:ml_hyperparameters}
\label{tab:hyperparameters}
\begin{tabular}{llll}
\toprule
\textbf{Model} & \textbf{Hyperparameter} & \textbf{Search Space} & \textbf{Distribution / Type} \\
\midrule
\textbf{Logistic Regression} & \texttt{solver} & 'saga' & Fixed \\
 & \texttt{max\_iter} & 1000 & Fixed \\
 & \texttt{C} & $[10^{-3}, 2.0]$ & Log-uniform \\
 & \texttt{l1\_ratio} & $[0.0, 1.0]$ & Uniform \\
 & \texttt{class\_weight} & $\{\text{'balanced'}, \text{None}\}$ & Categorical \\
 & \texttt{tol} & $[10^{-4}, 10^{-2}]$ & Log-uniform \\
\midrule
\textbf{LightGBM} & \texttt{objective} & 'multiclass' & Fixed \\
 & \texttt{n\_jobs} & 1 & Fixed \\
 & \texttt{num\_leaves} & $[20, 150]$ & Integer (Uniform) \\
 & \texttt{learning\_rate} & $[0.01, 0.2]$ & Log-uniform \\
 & \texttt{n\_estimators} & $[50, 150]$ & Integer (Uniform) \\
 & \texttt{feature\_fraction} & $[0.5, 1.0]$ & Uniform \\
\midrule
\textbf{LinearSVC} & \texttt{C} & $[10^{-3}, 2.0]$ & Log-uniform \\
 & \texttt{tol} & $[10^{-4}, 10^{-2}]$ & Log-uniform \\
 & \texttt{penalty} & $\{\text{'l1'}, \text{'l2'}\}$ & Categorical \\
 & \texttt{dual} & $\{\text{True}, \text{False}\}^*$ & Categorical \\
\midrule
\textbf{Random Forest} & \texttt{n\_estimators} & $[100, 250]$ & Integer (Log-uniform) \\
 & \texttt{max\_depth} & $[10, 30]$ & Integer (Uniform) \\
 & \texttt{max\_features} & $\{\text{'sqrt'}, \text{'log2'}\}$ & Categorical \\
\midrule
\textbf{Extra Trees} & \texttt{n\_estimators} & $[100, 250]$ & Integer (Log-uniform) \\
 & \texttt{max\_depth} & $[10, 30]$ & Integer (Uniform) \\
 & \texttt{max\_features} & $\{\text{'sqrt'}, \text{'log2'}\}$ & Categorical \\
\bottomrule
\multicolumn{4}{l}{\footnotesize $^*$ \textit{dual} was restricted to \text{False} when \text{penalty='l1'} to ensure mathematical compatibility.}
\end{tabular}
\end{table}

\clearpage

\section{AI usage declaration}\label{sec:ai-declaration}

In the boxes below, please specify which AI tool you used, to what extent, and for which task. If you did not use AI tools for the task, please state so by writing \textit{``I did not use any AI tools for this task''} in the corresponding box. Examples of AI tools to declare: ChatGPT, Claude, Gemini, Microsoft Copilot, Grammarly, DeepL, Google Translate, Perplexity, Elicit, Explainpaper, and ResearchRabbit. Example sentences:

\begin{itemize}
    \item Task: Review and editing of the text: \textit{``I used Grammarly for proofreading, revising the grammar of the text in \Cref{sec:introduction,sec:discussion}, and for rewording convoluted sentences throughout this work.''}
    \item Task: Structuring and planning the text: \textit{``I used Claude to suggest how my background description in \Cref{sec:introduction} should be structured and what should be included.''}
    \item Task: Coding: \textit{``I used the Microsoft Copilot extension in my IDE for code completion and for generating the code for the JavaScript frontend.''}
\end{itemize}

\vspace{.25em}

\begin{answerbox}{Task: Brainstorming and concept development}
    Tools used for the development of ideas, obtaining research goals, objectives, designing the research, and identifying and defining relevant concepts.
\end{answerbox}

\vspace{.25em}

\begin{answerbox}{Task: Literature review and analysis}
    Tools used for identifying relevant literature, reviewing potentially relevant literature,  and creating summaries.
\end{answerbox}

\vspace{.25em}

\begin{answerbox}{Task: Methodology}
    Tools used for identifying a suitable methodology and developing and adapting the methodology to the research question(s).
\end{answerbox}

\vspace{.25em}

\begin{answerbox}{Task: Coding}
     Tools used for writing and documenting code, algorithms, and software, testing and debugging, and understanding existing code.
\end{answerbox}

\vspace{.25em}

\begin{answerbox}{Task: Data collection, generation, and analysis}
     Tools used for collecting, generating, and processing primary or secondary data. Tools for qualitative or quantitative data analysis.
\end{answerbox}

\vspace{.25em}

\begin{answerbox}{Task: Interpretation and validation}
     Tools used for interpreting results, deriving implications, and assessing general replicability and reproducibility of the results or other research findings.  
\end{answerbox}

\vspace{.25em}

\begin{answerbox}{Task: Structuring and planning the text}
     Tools used for generating the outline of the work and/or the outline of specific sections.
\end{answerbox}

\vspace{.25em}

\begin{answerbox}{Task: Text generation}
     Tools used for generating text on various topics in different sections of the work, including titles and abstracts.
\end{answerbox}

\vspace{.25em}

\begin{answerbox}{Task: Translating text}
     Tools used for translating text written by the author or by others.
\end{answerbox}

\vspace{.25em}

\begin{answerbox}{Task: Review and editing of the text}
     Tools used for critically reviewing the written text, obtaining feedback, revising the content, structure, or grammar of the text, proofreading, rewording or paraphrasing the given text, and shortening or expanding the text.
\end{answerbox}

\vspace{.25em}

\begin{answerbox}{Task: Bibliography management}
     Tools used to create or format the reference list.
\end{answerbox}

\vspace{.25em}

\begin{answerbox}{Task: Other activities}
     Tools used for anything not covered by the given tasks.
\end{answerbox}

\clearpage

\section{Declaration of academic integrity}\label{sec:declaration}

I hereby declare that I have authored this work independently and without the use of any sources or assistance other than those specifically cited. All content taken verbatim or paraphrased from other sources has been clearly identified as such. Furthermore, I certify that this work has not been submitted, in its current or a similar form, for any other examination. All usage of AI tools has been fully disclosed in \Cref{sec:ai-declaration}, and I take full responsibility for all content generated with the help of AI. I am aware that any false or incomplete declarations given here or in \Cref{sec:ai-declaration} may be treated as attempts at deception and could thus lead to the failure of this examination. I am also aware that inability to provide in-depth technical explanations for any content contained in this work or in the underlying source code may be treated as evidence for illicit AI usage or misappropriation of the work of others and could thus lead to the failure of this examination.

\namesigdate{[Your name]}

\end{document}